[workspace]
members = [
    "common/*",
    "programs/*"
]

### cargo-build-sbf does not support workspace inheritance yet... Soon.

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
