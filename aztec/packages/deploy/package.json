{
  "name": "aztec",
  "version": "1.0.0",
  "main": "CardGame.js",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn clean && tsc -b",
    "build:dev": "tsc -b --watch",
    "clean": "rm -rf ./dest tsconfig.tsbuildinfo",
    "start": "yarn build && LOG_LEVEL='info: token' node ./dest/CardGame.js"
  },
  "dependencies": {
    "@aztec/accounts": "^0.81.0"
  }
}
