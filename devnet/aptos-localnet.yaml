apiVersion: v1
kind: Service
metadata:
  labels:
    app: aptos
  name: aptos
spec:
  ports:
    - name: node
      port: 8082
      targetPort: 8080
    - name: prometheus
      port: 6181
      targetPort: prometheus
  selector:
    app: aptos
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: aptos
  name: aptos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aptos
  serviceName: aptos
  template:
    metadata:
      labels:
        app: aptos
    spec:
      containers:
        - name: aptos-node
          image: aptos-node
          command:
            - /bin/sh
            - -c
            - /tmp/start_node.sh
          ports:
            - containerPort: 8080
              name: node
              protocol: TCP
            - containerPort: 6181
              name: prometheus
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: 6181

      restartPolicy: Always
